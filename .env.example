# Copy this file to .env.local and adjust if needed.
# Backend base URL (see ../run-api.bat). Without this, the app silently
# falls back to http://127.0.0.1:8000 and looks dead.
VITE_API_BASE_URL=http://127.0.0.1:8765
