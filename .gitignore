node_modules/
dist/
.vite/
src-tauri/target/
src-tauri/gen/
*.log
**/types.gen.ts
openapi.snapshot.check.json

# Env files — never committed, except the tracked template below.
.env
.env.*
!.env.example

# Kira avatar art — owner keeps this uncommitted (copied from the Python
# repo's assets/avatar/kira/ at apply-time, not checked into OpenCohost_UI).
public/avatar/

.atl/

# Real per-developer backend config (python_path, working_dir). The portable
# compile-time/bundle fallback is the tracked sibling backend.config.default.json.
src-tauri/backend.config.json

.codegraph/
